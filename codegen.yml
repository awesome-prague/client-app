overwrite: true
schema: 'http://localhost:4000'
documents: '**/*.graphql'
generates:
  lib/generated/types.d.ts:
    plugins:
      - 'typescript-common'
      - 'typescript-client'
      - 'fragment-matcher'
